import type { ProductsGridFiltersState } from './types';

export const initialFiltersState: ProductsGridFiltersState = {
  category: '',
  minPrice: '',
  maxPrice: '',
  minRating: '',
  maxRating: '',
};
